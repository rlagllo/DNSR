Restormer

pip install matplotlib scikit-learn scikit-image opencv-python yacs joblib natsort h5py tqdm
pip install einops gdown addict future lmdb numpy pyyaml requests scipy tb-nightly yapf lpips

Real-ESRGAN

pip install basicsr>=1.4.2 facexlib>=0.2.5 gfpgan>=1.3.5
pip install numpy opencv-python Pillow torch>=1.7 torchvision tqdm

KAIR / BSRGAN

pip install opencv-python scikit-image pillow torchvision hdf5storage ninja lmdb requests timm einops numpy tqdm natsort

ESRGCNN

pip install opencv-python scikit-image pillow torchvision hdf5storage ninja lmdb \
            requests timm einops numpy tqdm psutil Pillow